version: '3.8'

services:
  api:
    image: ghcr.io/luiziwasaki/rinha-2026-luiz:latest
    ports:
      - "9999:9999"
    environment:
      - PORT=9999
      - DATA_DIR=/data
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: "350MB"
